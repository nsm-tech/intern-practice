import React from "react";
import Layout from "../layout";
import Loc from "../features/location/loc";

const LocationPage: React.FC = () => {
  return (
    <Layout>
      <Loc />
    </Layout>
  );
};

export default LocationPage;
